bundle:
  name: behavior-apply-patch
  version: 1.0.0
  description: V4A diff-based file editing via apply_patch tool

tools:
  - module: tool-apply-patch
    source: ../modules/tool-apply-patch
    config:
      engine: native
      allowed_write_paths: ["."]
      denied_write_paths: []

context:
  include:
    - filesystem:context/editing-guidance.md
